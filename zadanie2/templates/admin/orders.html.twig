{% extends 'base.html.twig' %}

{% block title %}Admin - Zamówienia{% endblock %}

{% block body %}
<h1>Zarządzanie zamówieniami</h1>
<a href="/admin" class="btn btn-secondary mb-3">Powrót do panelu</a>
<table class="table table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>Klient</th>
            <th>Status</th>
            <th>Kwota</th>
            <th>Data</th>
            <th>Akcje</th>
        </tr>
    </thead>
    <tbody>
        {% for order in orders %}
        <tr>
            <td>{{ order.id }}</td>
            <td>{{ order.customerName }}</td>
            <td><span class="badge bg-{{ order.status == 'new' ? 'primary' : (order.status == 'completed' ? 'success' : 'secondary') }}">{{ order.status }}</span></td>
            <td>{{ order.totalPrice }} zł</td>
            <td>{{ order.createdAt|date('Y-m-d H:i') }}</td>
            <td>
                <a href="/admin/order/{{ order.id }}/edit" class="btn btn-sm btn-warning">Edytuj</a>
                <form action="/admin/order/{{ order.id }}/delete" method="POST" style="display:inline">
                    <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Usunąć?')">Usuń</button>
                </form>
            </td>
        </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}
